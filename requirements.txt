# PhoWhisper Dependencies
# Vietnamese-optimized speech recognition model

# Core dependencies
torch>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Optional: for faster inference
optimum>=1.8.0
