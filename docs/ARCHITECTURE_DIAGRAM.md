# Smart Home AI - Architecture Diagram

## 🏗️ Clean Architecture Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                            │
│                     go run cmd/assistant/main.go                 │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DELIVERY LAYER (cmd/)                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  cmd/assistant/main.go                                   │   │
│  │  - Dependency Injection                                  │   │
│  │  - Wire all components together                          │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                   USE CASE LAYER (internal/usecase/)             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  AssistantUseCase                                        │   │
│  │  - Orchestrates workflow                                 │   │
│  │  - Calls domain interfaces                               │   │
│  │  - No knowledge of implementation details                │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                  DOMAIN LAYER (internal/domain/)                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  ENTITIES (entity.go)                                    │   │
│  │  - MediaCapture                                          │   │
│  │  - Transcription                                         │   │
│  │  - AIResponse                                            │   │
│  │  - SpeechOutput                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  INTERFACES / PORTS (repository.go)                      │   │
│  │  - MediaCapturer                                         │   │
│  │  - SpeechRecognizer                                      │   │
│  │  - AIAssistant                                           │   │
│  │  - SpeechSynthesizer                                     │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│            INFRASTRUCTURE LAYER (internal/infrastructure/)       │
│  ┌──────────────────────┐      ┌─────────────────────────┐     │
│  │  OpenAI Client       │      │  FFmpeg Capturer        │     │
│  │  (openai/)           │      │  (media/)               │     │
│  │                      │      │                         │     │
│  │  Implements:         │      │  Implements:            │     │
│  │  - SpeechRecognizer  │      │  - MediaCapturer        │     │
│  │  - AIAssistant       │      │                         │     │
│  │  - SpeechSynthesizer │      │                         │     │
│  └──────────────────────┘      └─────────────────────────┘     │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                      EXTERNAL SERVICES                           │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────────┐    │
│  │  OpenAI      │  │  FFmpeg/     │  │  MacBook          │    │
│  │  APIs        │  │  AVFoundation│  │  Camera & Mic     │    │
│  └──────────────┘  └──────────────┘  └───────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Request Flow

```
1. User runs: go run cmd/assistant/main.go
                    ↓
2. main.go creates instances:
   - FFmpegCapturer (infrastructure/media)
   - OpenAI Client (infrastructure/openai)
   - ConsoleLogger (pkg/logger)
                    ↓
3. main.go creates AssistantUseCase
   with injected dependencies
                    ↓
4. AssistantUseCase.Execute() is called
                    ↓
5. Use case orchestrates:
   a. mediaCapturer.CaptureImage()
   b. mediaCapturer.RecordAudio()
   c. speechRecognizer.Transcribe()
   d. aiAssistant.AnalyzeMultimodal()
   e. speechSynthesizer.Synthesize()
   f. mediaCapturer.PlayAudio()
                    ↓
6. Each call goes through domain interfaces
   to infrastructure implementations
                    ↓
7. Infrastructure calls external services
   (OpenAI API, FFmpeg)
                    ↓
8. Results flow back up through layers
                    ↓
9. User sees/hears final output
```

## 🎯 Dependency Direction

```
External Services
       ↑
Infrastructure Layer  ← Implements interfaces
       ↑
Domain Layer          ← Defines interfaces (Ports)
       ↑
Use Case Layer        ← Uses interfaces
       ↑
Delivery Layer        ← Wires everything together
       ↑
User
```

**Key Principle**: Dependencies point INWARD
- Outer layers depend on inner layers
- Inner layers have NO knowledge of outer layers
- Domain layer is completely independent

## 🔌 Ports & Adapters Pattern

```
┌─────────────────────────────────────────────┐
│            DOMAIN (CORE)                     │
│                                              │
│  ┌──────────────────────────────────────┐  │
│  │  Port Interfaces                     │  │
│  │  - MediaCapturer                     │  │
│  │  - SpeechRecognizer                  │  │
│  │  - AIAssistant                       │  │
│  │  - SpeechSynthesizer                 │  │
│  └──────────────────────────────────────┘  │
│                                              │
└─────────────────────────────────────────────┘
         ↑                           ↑
         │                           │
    Implements                  Implements
         │                           │
┌────────┴────────┐         ┌────────┴────────┐
│  Adapter:       │         │  Adapter:       │
│  OpenAI Client  │         │  FFmpeg         │
│                 │         │  Capturer       │
│  (External API) │         │  (Local Tool)   │
└─────────────────┘         └─────────────────┘
```

## 📦 Module Dependencies

```
github.com/truong-nautilus/smart-home-ai
├── cmd/assistant
│   └── imports: internal/*, pkg/*
├── internal/usecase
│   └── imports: internal/domain
├── internal/infrastructure/openai
│   └── imports: internal/domain
├── internal/infrastructure/media
│   └── imports: internal/domain
├── internal/domain
│   └── imports: NONE (pure business logic)
└── pkg/logger
    └── imports: standard library only
```

## 🧩 Adding New Features

Want to add a new AI provider? Here's how:

```
1. Create new adapter:
   internal/infrastructure/anthropic/client.go

2. Implement domain interfaces:
   type AnthropicClient struct{}
   
   func (a *AnthropicClient) Transcribe(...) {...}
   func (a *AnthropicClient) AnalyzeMultimodal(...) {...}

3. Wire in main.go:
   anthropicClient := anthropic.NewClient(apiKey)
   
   usecase := NewAssistantUseCase(
       mediaCapturer,
       anthropicClient,  // ← Just swap here!
       anthropicClient,
       anthropicClient,
       logger,
   )

4. Done! No changes to business logic needed.
```

## 🎨 Benefits Visualization

```
Before (Monolithic):
┌─────────────────────────────┐
│      main.go (235 lines)    │
│  ┌─────────────────────┐   │
│  │ Business Logic +    │   │
│  │ OpenAI Code +       │   │
│  │ FFmpeg Code +       │   │
│  │ All Coupled Together│   │
│  └─────────────────────┘   │
└─────────────────────────────┘
❌ Hard to test
❌ Hard to change
❌ Hard to extend


After (Clean Architecture):
┌─────────────────────────────┐
│    Domain (Interfaces)      │ ← Pure business logic
└──────────┬──────────────────┘
           ↑
    ┌──────┴──────┐
    │   Use Case  │            ← Orchestration
    └──────┬──────┘
           ↑
    ┌──────┴──────────┐
    │  Infrastructure │        ← Implementation details
    └─────────────────┘
✅ Easy to test (mock interfaces)
✅ Easy to change (swap adapters)
✅ Easy to extend (add new adapters)
```

---

This architecture follows Robert C. Martin's Clean Architecture principles and Go best practices!
